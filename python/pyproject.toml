[project]
name = "qwen-tts-api"
version = "0.1.0"
description = "Python package exposing qwen-tts text-to-speech functionality as an API."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "qwen-tts>=0.0.5",
]

[dependency-groups]
dev = [
    "ruff>=0.14.14",
]
